/**
 * commitlint configuration. Enforces Conventional Commits.
 * See docs/conventions/commits.md for the project-specific rules.
 */
module.exports = {
  extends: ['@commitlint/config-conventional'],
};